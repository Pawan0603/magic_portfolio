---
title: "How to Integrate ChatGPT in a Next.js Web Application"
publishedAt: "2025-11-13"
image: "/images/blog/chatgpt.png"
summary: "A step-by-step guide on integrating OpenAI ChatGPT into your Next.js app using the latest OpenAI API, with clean API routes and a functional chat UI."
tag: "Tutorial"
---

Integrating ChatGPT into your Next.js application unlocks powerful AI features for your users—such as chatbots, summarizers, generators, translators, and more. In this guide, I'll show you how to integrate the **OpenAI ChatGPT API** into a Next.js project in the simplest and cleanest way.

---

## Step 1: Install the OpenAI SDK

Start by installing the official OpenAI Node.js SDK:

<CodeBlock className="my-24"
    compact
    codeInstances={[{
        code: "npm install openai",
        label: "Terminal",
        language: "bash"
    }]}
    copyButton
/>

---

## Step 2: Configure OpenAI Client

Create a file `lib/openai.js` to initialize the OpenAI client once and reuse it throughout your app:

<CodeBlock className="my-24"
    compact
    codeInstances={[{
        code: `import OpenAI from "openai";

const client = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

export default client;`,
        label: "openai.js",
        language: "js"
    }]}
    copyButton
/>

---

## Step 3: Create a ChatGPT API Route

Inside your Next.js project, create an API route:

For the **App Router**:  
`app/api/chat/route.js`

<CodeBlock className="my-24"
    compact
    codeInstances={[{
        code: `import client from "@/lib/openai";

export async function POST(req) {
  try {
    const { message } = await req.json();

    const response = await client.chat.completions.create({
      model: "gpt-4o-mini",
      messages: [
        { role: "system", content: "You are a helpful AI assistant." },
        { role: "user", content: message },
      ],
    });

    return Response.json({
      reply: response.choices[0].message.content,
    });

  } catch (error) {
    return Response.json(
      { error: error.message || "Something went wrong" },
      { status: 500 }
    );
  }
}`,
        label: "route.js",
        language: "js"
    }]}
    copyButton
/>

---

## Step 4: Build a Simple Chat UI

Here’s a clean chat interface using React Client Components:

Inside: `app/chat/page.jsx`

<CodeBlock className="my-24"
    compact
    codeInstances={[{
        code: `"use client";

import { useState } from "react";

export default function ChatPage() {
  const [message, setMessage] = useState("");
  const [reply, setReply] = useState("");
  const [loading, setLoading] = useState(false);

  const sendMessage = async () => {
    setLoading(true);

    const res = await fetch("/api/chat", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ message }),
    });

    const data = await res.json();
    setReply(data.reply);
    setLoading(false);
  };

  return (
    <div className="p-6 max-w-xl mx-auto">
      <h1 className="text-3xl font-semibold mb-4">ChatGPT Integration</h1>

      <textarea
        className="w-full p-3 border rounded-md"
        rows="4"
        value={message}
        onChange={(e) => setMessage(e.target.value)}
        placeholder="Type a message..."
      />

      <button
        className="mt-3 px-4 py-2 bg-blue-600 text-white rounded-md"
        onClick={sendMessage}
        disabled={loading}
      >
        {loading ? "Generating..." : "Send"}
      </button>

      {reply && (
        <div className="mt-4 p-3 bg-gray-100 rounded-md">
          <strong>ChatGPT:</strong> {reply}
        </div>
      )}
    </div>
  );
}`,
        label: "page.jsx",
        language: "js"
    }]}
    copyButton
/>

---

## Step 5: Add Your OpenAI API Key

Add this into your `.env.local`:

<CodeBlock className="my-24"
    compact
    codeInstances={[{
        code: "OPENAI_API_KEY=your-secret-openai-api-key",
        label: ".env.local",
        language: "env"
    }]}
    copyButton
/>

> ⚠️ Never push `.env.local` to GitHub.

---

## Bonus: Enable Streaming (Typing Effect)

If you want ChatGPT replies like the real ChatGPT (typing effect), I can also provide a **streaming version** using `ReadableStream`.  
Just tell me — I’ll add it.

---

## Conclusion

Congratulations! 🎉  
You’ve successfully integrated ChatGPT into your Next.js application. With this setup, you can now build:

- AI chatbots  
- Content generators  
- Code helpers  
- Text summarizers  
- Translation tools  
- SEO content machines  

Next.js + OpenAI opens endless possibilities to enhance your app with artificial intelligence.

Stay tuned for more advanced features and tutorials!